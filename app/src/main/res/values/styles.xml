<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="windowNoTitle">true</item>
        <item name="windowActionBar">false</item>
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="ToolBarOverflowStyle" parent="AppTheme">
        <item name="actionOverflowButtonStyle">@style/WeixinOverflowButtonStyle</item>
    </style>

    <style name="WeixinOverflowButtonStyle">
        <item name="android:src">@mipmap/actionbar_add_icon</item>
    </style>
</resources>
